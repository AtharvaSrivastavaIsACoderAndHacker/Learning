#include<iostream>

using namespace std;

int main(int argc, char const *argv[]){
    
    // Variable names can be 0-255 characters long !
    // must begin with either an alphabet or _ , not with a number !
    // can contain letters, numbers and _s !
    // they are case-sensitive, int sum = 0; and int sUm = 0; are different variables !
    // no whitespaces or special characters are allowed !
    // a C++ keyword (reserved words like int, void, return etc.) can't be used as an identifier !
    
    return 0;
}