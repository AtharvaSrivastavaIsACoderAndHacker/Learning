#include<iostream>

using namespace std;

int main(int argc, char const *argv[]){
    
    // Operator precedence decides which operator will get preference when many in the same line !
    // When operators with same precedence are in one line, they are executed leftToRight or rightToLeft, it's called associativity !
    
    https://en.cppreference.com/w/cpp/language/operator_precedence --> Operator precedence and Associativity in C++ table !

    return 0;
}