int main(int argc, char const *argv[]){
    
    // In C++, exists variables like any other language
    // Datatypes are of three types:-
        // * Built-in ---> int, char, float, double, bool ----> bool is new in c++, c didn't have booleans
        // * User defined ---> struct, union and enum ----> enum is new, c only had struct and union not enum !
        // * Derived ---> array, functions and pointers !

    // bool and enum are 2 new dataypes that werent there in c language
    // enum is explained in later files !
    
    return 0;
}