# Hierarchical Inheritance

class BaseClass:
      pass

class Derived1(BaseClass):
      pass

class Derived2(BaseClass):
      pass

class Derived3(BaseClass):
      pass


# Rule : Hierarchy Should Always Continue Without Fail ! If It Discontinues, Then It's Not Hierarchical Inheritance !